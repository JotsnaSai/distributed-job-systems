version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: job-postgres
    environment:
      POSTGRES_DB: jobdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"